// Ensure that the select field for items is more user-friendly
document.addEventListener("DOMContentLoaded", function () {
    const itemSelect = document.getElementById('items');

    // If you need to add some custom behavior when selecting items, you can modify this area.
    itemSelect.addEventListener('click', function () {
        // This space can be used for further improvements or notifications if needed.
        // Example: alert("You can select multiple items using Ctrl or Shift keys.");
    });
});

